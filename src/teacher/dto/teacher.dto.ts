
//this is for creating a dynamic object for the request body, 
//so we dont have to update the actual controller if we add a new field to the request body
export class FindTeacherResponseDto {
    id: string;
    name: string;
    
}

